<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.se_project.mapper.IVerificationCodeMapper">
    <select id="getVerificationCode">
        select * from werwerTrip.verificationCodes
        where email = #{email}
    </select>

    <insert id="addVerificationCode">
        insert into werwerTrip.verificationCodes (email, code, generatedAt, expiresAt)
        values (#{email}, #{code}, #{generateAt}, #{expiresAt})
        on duplicate key update
                             code = values(code),
                             generatedAt = values(generatedAt),
                             expiresAt = values(expiresAt)
    </insert>
</mapper>