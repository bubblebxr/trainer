<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.se_project.mapper.IFoodMapper">
    <insert id="addFoodOrder">
        insert into foodOrders(oid, foodName, count, trainId, mealTime, mealDate, photo)
        VALUES
            (#{oid}, #{foodName}, #{count}, #{trainId},  #{mealTime}, #{mealDate}, #{photo})
    </insert>
</mapper>